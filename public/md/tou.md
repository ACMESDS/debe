[my link](/junk.txt)
[](/plot.view?src=regress&id=1&x=Save_cls[0].weights&y=Save_cls[0].weights)

urls: ${JSON.stringify( urls )}
	
now: ${now}
	
by: ${by}

register: ${register}

input: ${input}

query a,b,c: ${a}, ${b}, ${c}