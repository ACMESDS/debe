//- UNCLASSIFIED

extends site.jade
append site_help
	p Welcome to #{title}.
append site_parms
	- view = query.option || "Min"
	- parms = {}
append site_body

	#grid.Changes(
		path="/history.db?return=changes",
		head="Datasets,Print,Refresh",
		page=100,dims=dims,
		cols="Moderators,Datasets.t")
		
	#grid.Signoffs(
		path="/history.db?return=signoffs",
		head="Datasets,Execute,Print,Refresh",
		page=100,dims=dims,
		links="Hawk,Datasets,Power,Comment,Approved",
		cols="Approved.c,Moderator.t,Datasets.t,Comment.x,DetailedComment.h")

	//
		#grid.Itemized(
			path="/history.db?return=itemized",
			head="Datasets,Print,Refresh",
			page=100,dims=dims,
			cols="Moderators,Idx.t.Index")

		#grid.Earnings(
			path="/history.db?return=earnings",
			head="Print,Refresh",
			page=100,dims=dims,
			cols="Client,Earnings.t,Comments.T")

		#grid.Moderators(
			path="/moderators.db",
			page=100,dims=dims,
			cols="Hawk.t.Role,Dataset,Field,Power.i")

//- UNCLASSIFIED
